<?xml version="1.0" encoding="UTF-8"?>

<!-- Generated by MavenOwnArchetype_JavaBeans v3.1.1 -->
<!--   Updated to MavenOwnArchetype_JavaBeans v4.1.0 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.rutar</groupId>
    <artifactId>JImageView</artifactId>
    <version>1.1.0</version>
    <packaging>jar</packaging>

    <name>JImageView</name>
    <url>https://github.com/RutarAndriy/JImageView</url>

    <developers>
        <developer>
            <id>rutar</id>
            <name>Rutar Andriy</name>
            <email>RutarAndriy@gmail.com</email>
        </developer>
    </developers>

    <properties>
        <!-- Кодування вихідного коду -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Версія Java, на основі якої збирається проект -->
        <maven.compiler.release>21</maven.compiler.release>
        <!-- Головний клас програми -->
        <app.mainClass>com.rutar.jimageview.JImageViewDemo</app.mainClass>
    </properties>
    
    <repositories>

        <!-- Репозиторій JitPack.IO -->
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>

    </repositories>

    <dependencies>
        
        <!-- Виконання системних команд -->
        <dependency>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>3.2.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- JUnit_5 - core -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.8.2</version>
            <scope>test</scope>
        </dependency>

        <!-- JUnit_5 - engine -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.8.2</version>
            <scope>test</scope>
        </dependency>

        <!-- JUnit_5 - параметричні тести -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>5.8.2</version>
            <scope>test</scope>
        </dependency>
        
    </dependencies>

    <build>
        <plugins>
            
            <!-- Збирання jar-файлу -->
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>false</addClasspath>
                            <mainClass>${app.mainClass}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!-- Збирання jar-файлу із залежностями -->
            <plugin>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <minimizeJar>true</minimizeJar>
                    <shadedArtifactAttached>true</shadedArtifactAttached>
                    <shadedClassifierName>jar-with-dependencies</shadedClassifierName>
                    <createDependencyReducedPom>false</createDependencyReducedPom>
                    <transformers>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <mainClass>${app.mainClass}</mainClass>
                        </transformer>
                    </transformers>
                    <filters>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/maven/**</exclude>
                                <exclude>META-INF/*.txt</exclude>
                            </excludes>
                        </filter>
                        <!--
                        <filter>
                            <artifact>[groupId]:[artifactId]</artifact>
                            <includes>
                                <include>com/rutar/example/**</include>
                            </includes>
                            <excludes>
                                <exclude>com/rutar/example/Example*</exclude>
                            </excludes>
                        </filter>
                        -->
                    </filters>
                </configuration>
                <executions>
                    <execution>
                        <id>make-shade</id>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Налаштування компіляції -->
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.12.1</version>
                <configuration>
                    <!--
                    <compilerArgs>
                        <arg>-Xlint:unchecked</arg>
                    </compilerArgs>
                    -->
                </configuration>
            </plugin>

            <!-- Налаштування та запуск тестів -->
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.2</version>
                <dependencies>
                    <dependency>
                        <groupId>me.fabriciorby</groupId>
                        <artifactId>maven-surefire-junit5-tree-reporter</artifactId>
                        <version>1.2.1</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <reportFormat>plain</reportFormat>
                    <consoleOutputReporter>
                        <disable>true</disable>
                    </consoleOutputReporter>
                    <statelessTestsetInfoReporter implementation="org.apache.maven.plugin.surefire.extensions.junit5.JUnit5StatelessTestsetInfoTreeReporter">
                        <theme>UNICODE</theme>
                    </statelessTestsetInfoReporter>
                </configuration>
            </plugin>

            <!-- Створення JavaDoc документації -->
            <plugin>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.6.3</version>
                <configuration>
                    <doclint>-missing</doclint>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Вимкнення стандартного deploy-плагіна -->
            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.1</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <!-- Запуск системних команд -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.2.0</version>
                <executions>

                    <!-- Робимо скрипт виконуваним -->
                    <execution>
                        <id>command_1</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>chmod</executable>
                            <commandlineArgs>+x .create-github-release.sh</commandlineArgs>
                        </configuration>
                    </execution>

                    <!-- Запуск скрипта створення релізів на GitHub -->
                    <execution>
                        <id>command_2</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>./.create-github-release.sh</executable>
                            <commandlineArgs>${project.artifactId} ${project.version}</commandlineArgs>
                        </configuration>
                    </execution>

                </executions>
            </plugin>
        </plugins>
    </build>
</project>
